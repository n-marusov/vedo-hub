= Architecture
:description: Microservices structure, service inventory, and communication patterns.

== Overview

VEDO Core uses a *microservices architecture* organized as a monorepo. 14+ loosely coupled, independently deployable services — each owning its data, communicating through well-defined APIs (REST/gRPC/events), and scalable independently.

The architecture was chosen because the project requires polyglot persistence (Neo4j, PostgreSQL, Redis), different scaling profiles per service (ontology queries need more CPU than auth), and independent team ownership across Go, Rust, Python, and TypeScript codebases.

== Directory Structure

[source]
----
vedo-core/
├── src/
│   ├── services/
│   │   ├── api-gateway/           # Go — Entry point, routing, auth proxy
│   │   ├── auth-service/          # Go — Keycloak integration, RBAC
│   │   ├── ontology-service/      # Rust — Neo4j CRUD, graph operations
│   │   ├── versioning-service/    # Rust — Git-like commits, branches, diff
│   │   ├── metrics-service/       # Python — Analytics, aggregation
│   │   ├── publisher-service/    # Rust — Ontology publishing pipeline
│   │   ├── public-browse-api/    # Rust — Public read-only API
│   │   ├── commenting-service/   # Go — Comments & collaboration
│   │   ├── ticket-api/            # Go — Ticket management CRUD
│   │   ├── ticket-classifier/    # Python — Auto-classification (ML)
│   │   ├── ticket-notifier/      # Go — Email / WebSocket notifications
│   │   ├── ticket-sync/          # Go — External system sync
│   │   ├── ticket-telemetry-listener/ # Go — Telemetry ingestion
│   │   ├── support-service/      # Go — Support operations
│   │   ├── frontend/             # Vue 3 — Main web UI (BFF pattern)
│   │   └── publish-browse-ui/   # Vue 3 — Published ontology viewer
│   ├── cli/                      # Go — Operator CLI (vedo-cli)
│   ├── docker/                   # Multi-stage Dockerfile templates
│   ├── templates/               # Service scaffolds by language
│   └── build/                   # Makefile includes per language
├── deploy/                       # Infrastructure-as-Code
│   ├── docker-compose.yml       # All-services orchestration
│   ├── ci/gitlab-ci.yml         # CI/CD pipeline
│   ├── helm/                    # Kubernetes Helm values
│   ├── keycloak/                # Realm configuration
│   └── observability/           # OTEL, Grafana, Prometheus, Loki, Tempo
├── tests/                        # Cross-service test suites
└── specs/                        # Specifications (submodule)
----

== Service Inventory

=== Go Services

|===
|Service |Port |Responsibility

|`api-gateway` |8080 |Entry point, routing, auth proxy, BOLA/BFLA enforcement
|`auth-service` |8084 |Keycloak integration, role-based access control
|`ai-orchestration-service` |— |LLM orchestration, policy enforcement, audit logging, prompt templates
|`commenting-service` |— |Comments, node locking, real-time collaboration
|`ticket-api` |— |Ticket management CRUD
|`ticket-notifier` |— |Email / WebSocket notifications
|`ticket-sync` |— |External system synchronization
|`ticket-telemetry-listener` |— |Telemetry ingestion
|`support-service` |— |Support operations
|===

=== Rust Services

|===
|Service |Port |Responsibility

|`ontology-service` |8081 |Neo4j CRUD, graph operations, imports/exports
|`versioning-service` |8082 |Git-like commits, branches, diffs (PostgreSQL)
|`publisher-service` |— |Ontology publishing pipeline
|`public-browse-api` |— |Public read-only API for published ontologies
|===

=== Python Services

|===
|Service |Port |Responsibility

|`document-extractor` |— |AI document extraction, NL to OWL conversion
|`metrics-service` |8086 |Analytics, aggregation (RabbitMQ consumer)
|`ticket-classifier` |— |Auto-classification using ML
|===

=== Frontend Services

|===
|Service |Port |Responsibility

|`frontend` |3000 |Main web UI (Vue 3 + Apollo Client + Vue Flow)
|`publish-browse-ui` |— |Published ontology public viewer
|===

=== Shared Libraries

|===
|Library |Language |Purpose

|`apps/shared/llm` |Go |LLM provider abstraction, registry, prompt templates, OTEL observability
|`apps/shared/proto` |Go/Rust |gRPC protobuf definitions
|`apps/shared/src` |Rust |Health, metrics, tracing, protos
|===

== Communication Patterns

|===
|Pattern |Usage

|Synchronous (REST/gRPC) |API Gateway proxies to backend services; ticket-api serves CLI/HTTP
|Asynchronous (RabbitMQ) |Metrics ingestion, ticket classification, notifications
|WebSocket |Real-time collaboration — commenting, node locking
|GraphQL (Apollo) |Frontend → API Gateway for complex data queries
|===

[source]
----
Frontend ──(GraphQL/REST)──> API Gateway ──(REST/gRPC)──> Backend Services
                                   │
                                   ├─> ontology-service ──> Neo4j
                                   ├─> versioning-service ──> PostgreSQL
                                   ├─> auth-service ──> Keycloak
                                   ├─> ai-orchestration-service ──> LLM providers
                                   └─> ...
                               WebSocket ──> commenting-service
                               RabbitMQ ──> metrics-service, ticket-classifier
----

== Key Patterns

* **gRPC** for inter-service communication, REST for external API, WebSocket for real-time
* **Structured logging** with OpenTelemetry trace context propagation across all services
* **Health check endpoints** (`/health`, `/ready`) on every service
* **Multi-stage Docker builds** (Go: golang→alpine, Rust: rust→debian-slim, Python: python-slim, TS: node→nginx)

== Key Principles

. *Service Autonomy* — each service owns its data, runtime, and deployment.
. *API-First Contracts* — interfaces defined as contracts before implementation.
. *Smart Endpoints, Dumb Pipes* — business logic lives inside services; the gateway routes but does not orchestrate.
. *Design for Failure* — circuit breakers, retries with backoff, timeouts, fallback strategies at the gateway level.
. *Polyglot Persistence* — Neo4j for graphs, PostgreSQL for version history, Redis for caching/locks.
. *Observability by Default* — OpenTelemetry trace context propagated through all services; structured JSON logging with `trace_id` and `request_id`.

== Health Checks

Every service exposes `/health` and `/ready` endpoints:

[source,json]
----
// GET /health
{"status": "healthy"}

// GET /ready
{"status": "ready"}
----

Prometheus metrics are available at `/metrics`.

== Anti-Patterns to Avoid

[cols="1,4"]
|===
|❌ *Distributed Monolith* |Services coupled via synchronous calls requiring coordinated deploys
|❌ *Shared Database* |Multiple services accessing the same Neo4j/PostgreSQL tables
|❌ *Nano-services* |Services too small that operational overhead exceeds business value
|❌ *Leaky Gateway* |API Gateway containing business logic instead of routing
|===

== See Also

* xref:getting-started.adoc[Getting Started] — prerequisites and quick start
* xref:deployment.adoc[Deployment] — Docker setup and environments
* xref:configuration.adoc[Configuration] — environment variables per service
* xref:testing.adoc[Testing] — test suites