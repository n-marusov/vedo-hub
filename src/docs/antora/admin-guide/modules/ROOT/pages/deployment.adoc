= Deployment
:description: Docker Compose, Helm, and CI/CD pipeline for VEDO Core.

== Docker Compose (Development)

The fastest way to run the full stack:

[source,bash]
----
cd src && make docker-up     # Start all 28 containers
cd src && make docker-down   # Stop all containers
cd src && make docker-logs   # Tail logs from all services
cd src && make docker-ps     # List running containers
----

=== Open a Shell in a Container

[source,bash]
----
make docker-shell SVC=ontology-service
make docker-shell SVC=api-gateway
----

=== Build All Docker Images

[source,bash]
----
cd src && make docker-build
----

Multi-stage Dockerfiles per language in `src/services/*/Dockerfile`:

|===
|Language |Base → Final

|Go |`golang` → `alpine`
|Rust |`rust` → `debian-slim`
|Python |`python-slim`
|TypeScript |`node` → `nginx`
|===

== Port Reference

All host ports are configurable via environment variables in `.env.dev`, `.env.test`, and `.env.staging`.

=== Convention

* `XXX_PORT` — host port (changes between environments)
* `XXX_CONTAINER_PORT` — container port (same across all environments)
* Dev: default ports (no offset)
* Test: +10000 to all host ports (RabbitMQ AMQP: +10001)
* Staging: +20000 to all host ports (RabbitMQ AMQP: +20002)

=== Application Services

[cols="2,2,2,2,4",options="header"]
|===
|Service |Dev |Test (+10000) |Staging (+20000) |Notes

|Frontend (SPA)
|3000
|13000
|23000
|Vue 3 SPA with hot-reload

|Publish Browse UI
|3002
|13002
|23002
|Public ontology viewer (Vue 3)

|API Gateway
|8080
|18080
|28080
|REST/GraphQL entry point

|Auth Service (gRPC)
|9003
|19003
|29003
|Keycloak integration, RBAC

|Versioning Service (gRPC)
|9002
|19002
|29002
|Git-like commits, branches, diff

|Metrics Service
|8084
|18084
|28084
|Analytics and aggregation

|Publisher Service (gRPC)
|9005
|19005
|29005
|Ontology publishing pipeline

|Public Browse API (gRPC)
|9011
|19011
|29011
|Public read-only API

|Commenting Service (gRPC)
|9004
|19004
|29004
|Comments and collaboration

|Ticket API (REST)
|8088
|18088
|28088
|Ticket management CRUD

|Ticket API (gRPC)
|9010
|19010
|29010
|Ticket management gRPC

|Ticket Classifier
|8089
|18089
|28089
|Auto-classification ML

|Ticket Telemetry Listener
|8090
|18090
|28090
|Telemetry ingestion

|Ticket Notifier
|8091
|18091
|28091
|Email/WebSocket notifications

|Document Extractor (gRPC)
|9013
|19013
|29013
|AI document extraction \& NL→OWL

|AI Orchestration (health)
|8093
|18093
|28093
|LLM orchestration health

|AI Orchestration (gRPC)
|9014
|19014
|29014
|LLM orchestration gRPC

|===

=== Infrastructure

[cols="2,2,2,2,4",options="header"]
|===
|Service |Dev |Test (+10000) |Staging (+20000) |Notes

|Neo4j (HTTP)
|7474
|17474
|27474
|Neo4j Browser UI

|Neo4j (Bolt)
|7687
|17687
|27687
|Neo4j Bolt protocol

|PostgreSQL
|5432
|15432
|25432
|Version history, org data

|Redis
|6379
|16379
|26379
|Cache and locks

|RabbitMQ (AMQP)
|5672
|15673
|25674
|Message queue (AMQP offset +10001/+20002)

|RabbitMQ (Management)
|15672
|25672
|35672
|RabbitMQ management UI

|MinIO (API)
|9000
|19000
|29000
|S3-compatible object storage

|MinIO (Console)
|9001
|19001
|29001
|MinIO admin console

|Keycloak
|8180
|18180
|28180
|OIDC/OAuth2 (container: 8080)

|===

=== Observability (profile: `obs`)

[cols="2,2,2,2,4",options="header"]
|===
|Service |Dev |Test (+10000) |Staging (+20000) |Notes

|Prometheus
|9090
|19090
|29090
|Metrics collection

|Grafana
|3001
|13001
|23001
|Dashboards (container: 3000)

|Loki
|3100
|13100
|23100
|Log aggregation

|Tempo (HTTP)
|3200
|13200
|23200
|Tracing HTTP endpoint

|Tempo (OTLP gRPC)
|4317
|14317
|24317
|OTLP gRPC receiver

|Tempo (OTLP HTTP)
|4318
|14318
|24318
|OTLP HTTP receiver

|OTEL Collector (Prometheus)
|8888
|18888
|28888
|Prometheus metrics export

|OTEL Collector (Health)
|8889
|18889
|28889
|Collector health endpoint

|===

=== LLM (profile: `llm`)

[cols="2,2,2,2,4",options="header"]
|===
|Service |Dev |Test (+10000) |Staging (+20000) |Notes

|Ollama
|11434
|21434
|31434
|Local LLM server

|===

=== Documentation (profile: `documentation`)

[cols="2,2,2,2,4",options="header"]
|===
|Service |Dev |Test (+10000) |Staging (+20000) |Notes

|User Guide
|5000
|15000
|25000
|Antora user guide site

|Developer Guide
|5001
|15001
|25001
|Antora developer guide site

|Admin Guide
|5002
|15002
|25002
|Antora admin guide site

|Integrator Guide
|5003
|15003
|25003
|Antora integrator guide site

|===

== Kubernetes (Helm)

Helm chart values are in `deploy/helm/`:

[source,bash]
----
helm install vedo-core deploy/helm/ -f deploy/helm/values.yaml
----

|===
|Area |Path |Description

|Values |`deploy/helm/values.yaml` |Service replicas, resources, ingress
|Charts |`deploy/helm/` |Helm chart templates
|===

== CI/CD Pipeline

The pipeline is defined in `deploy/ci/gitlab-ci.yml` and runs on GitLab CI.

=== Pipeline Stages

[source,bash]
----
# Run all CI checks locally
cd src && make ci
# Runs: build + lint + test + typecheck
----

|===
|Stage |Commands |Description

|Build |`make build` |Compile all services (Rust, Go, Python, TypeScript)
|Lint |`make lint` |Linters for all languages + YAML
|Test |`make test` |Unit and integration tests
|Typecheck |`make typecheck` |TypeScript type checking
|Docker |`make docker-build` |Build Docker images for all services
|Security |SAST / DAST |Security scanning gates + dependency scanning
|Deploy |— |Deploy to target environment
|===

== Security Gates

|===
|Gate |Type |Description

|SAST |Static analysis |Source code vulnerability scanning
|DAST |Dynamic analysis |Running application vulnerability scanning
|Dependency scanning |Supply chain |Known CVEs in dependencies
|BOLA/BFLA tests |Authorization |Object-level and function-level access control tests
|===

Security test suites are in `tests/security/`.

== Health Checks

Every service container has a health check configured in `docker-compose.yml`:

[source,yaml]
----
healthcheck:
  interval: 10s
  timeout: 5s
  retries: 3
  test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:8080/health"]
----

Compose waits for `condition: service_healthy` before starting dependent services.

== Organizational Database

The `vedo_org` PostgreSQL database stores group/project hierarchy, role memberships, attribute policies, and audit events. It is created automatically via the init script at `deploy/postgres/init/01-create-org-db.sql`.

The `auth-service` requires the `AUTH_SERVICE_DATABASE_URL` environment variable to connect:

[source,bash]
----
AUTH_SERVICE_DATABASE_URL=postgres://postgres:password@postgres:5432/vedo_org?sslmode=disable
----

Migrations are applied automatically on auth-service startup (see `src/services/auth-service/migrations/`). If PostgreSQL is unavailable, auth-service falls back to an in-memory store for development.

== See Also

* xref:observability.adoc[Observability] — tracing, metrics, and logging
* xref:security.adoc[Security] — Keycloak realm, BOLA/BFLA, security configuration
* xref:index.adoc[Admin Guide Home]
