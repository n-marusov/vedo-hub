= Integrator Guide

Integration entry points, API contracts, and authentication for external teams connecting to VEDO Core.

== Guides

* xref:api-reference.adoc[API Reference] — REST/GraphQL endpoints and error codes
* xref:authentication.adoc[Authentication] — Keycloak OIDC/OAuth2 and identity providers
* xref:integration-config.adoc[Integration Configuration] — service URLs, ports, and rate limits

== API Surface

External routes are exposed through API Gateway under `/api/v1`. All requests require a valid JWT token obtained via Keycloak.