# @ctx: Observability stack secrets for docker-compose.observability.yaml
# @hlv:sec [SECRET_HANDLING] — do not commit real values; use .env.observability.local for overrides
# Source: llm/src/docker-compose.observability.yaml reads these via env_file

# Grafana admin credentials
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
