version: v2
modules:
  - path: .
    name: buf.build/vedo-core/proto
    lint:
      use:
        - STANDARD
      except:
        - PACKAGE_VERSION_SUFFIX
        - ENUM_VALUE_PREFIX
        - PACKAGE_DIRECTORY_MATCH
    breaking:
      use:
        - FILE
deps:
  - buf.build/googleapis/googleapis
