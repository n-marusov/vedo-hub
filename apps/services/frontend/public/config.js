// Placeholder — overwritten by docker-entrypoint.sh at container startup.
// In dev mode (vite), the dev-runtime-config plugin in vite.config.ts
// intercepts /config.js and serves a dynamically generated config instead.
// See apps/services/frontend/vite.config.ts and .env.development for dev config.
window.__VEDO_CONFIG__ = { _placeholder: true };
