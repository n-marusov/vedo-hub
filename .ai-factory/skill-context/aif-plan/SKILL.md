# Project Rules for $aif-plan

> Auto-generated by `$aif-evolve`. Do not edit manually.
> Last updated: 2026-08-02 00:25
> Based on: 6 analyzed patches (2026-07-18-18.23.md, 2026-07-23-16.10.md, 2026-07-26-22.18.md, 2026-07-27-15.30.md, 2026-07-29-18.30.md, 2026-07-31-12.29.md) + $aif-test-quality skill integration

## Rules

### TQS Gate in Acceptance Criteria

**Source**: `$aif-test-quality` skill, `.ai-factory/rules/test-quality.md`
**Rule**: When a plan includes test files in any task's "Files to create" section, the plan's **acceptance criteria** MUST include a test quality gate:

```markdown
## Acceptance Criteria
- [ ] All tests pass: `go test ./...` / `cargo test` / `pytest` / `vitest`
- [ ] Test Quality Score (TQS) ≥ bronze (6.0)
- [ ] No B1–B7 anti-patterns (see .ai-factory/rules/test-quality.md)
- [ ] Traceability annotations present (`// Validates: REQ-...`)
```

**Why:** Without explicit acceptance criteria, tests can be written with anti-patterns and still pass verification. The TQS gate makes quality measurable.

### traceability.ttl Update in Implementation Steps

**Source**: `.ai-factory/RULES.md` Traceability §1, `$aif-test-quality` skill
**Rule**: When a plan creates or modifies test files, the plan MUST include an explicit step:

```markdown
### Step N: Update traceability.ttl
- Add `vdo:TestSuite` entry for each new test file
- Add `vdo:validates` triples for each `// Validates: REQ-...` annotation
- Remove stale triples for deleted/renamed test files
```

Place this step AFTER the test implementation step(s) but BEFORE the final commit.

**Why:** RULES.md mandates traceability.ttl stays in sync. Without an explicit plan step, it gets forgotten.

### BDD Naming Convention Reference

**Source**: `.ai-factory/rules/test-quality.md`
**Rule**: Plan task descriptions for test-writing subtasks MUST reference the BDD naming convention:

- Go/Rust/Python: `[Condition]_[Action]_[ExpectedResult]`
- TypeScript: `'should <expected> when <condition>'`

Include this as a note in the task description:
```markdown
> BDD naming: `[Condition]_[Action]_[ExpectedResult]`
> Anti-patterns: see `.ai-factory/rules/test-quality.md`
```

### No-Tests Services Policy

**Source**: `$aif-test-quality` skill
**Rule**: For services with 0 tests (e.g., `frontend`, `document-extractor`):
- TQS = N/A — do NOT BLOCK the plan for missing tests
- But: if the plan ADDS tests to a previously untested service, those tests MUST reach bronze (≥ 6.0)
- Add to plan: "New tests must meet minimum quality (TQS ≥ bronze)"

### Security Test Tasks in Plan

**Source**: `2026-07-18-18.23.md` — M2 AI Ontology Creation (auth bypasses, cross-ontology data corruption)
**Rule**: When planning security-related tasks (auth, RBAC, input validation, prompt injection, encryption, audit, cross-ontology scope), include an explicit subtask for writing negative tests that prove the bypass is closed. Reference `tests/security/` or the service's existing test pattern. The test must dispatch a real HTTP request, not use mock assertions. This rule is cumulative with the existing TQS Gate — security tests must also meet minimum quality (TQS ≥ bronze).

### Design Tasks — Library Component First

**Source**: `2026-07-23-16.10.md` (dangling B:ID refs in design pages)
**Rule**: When a plan includes pages referencing design-system components, order the tasks so the library component is created FIRST in `design/ui-kit.lib.pen` with the confirmed id (e.g. `ze4Nt`, `LgxGO`), and the page references that confirmed id afterwards. Treat a dangling `B:<id>` ref like an unresolved import — add a referential-integrity audit step for `design/**/*.pen` (`.ai-factory/scripts/_analyze_refs.js`).

### Frontend API-Layer Refactors Include E2E Test Updates

**Source**: `2026-07-26-22.18.md` (dashboard GraphQL→REST), `2026-07-27-15.30.md` (validation REST migration)
**Rule**: When planning a frontend API-layer migration (GraphQL→REST or vice versa), include a task for updating Playwright mocks and E2E tests in the SAME milestone — the plan must not defer test updates to a later milestone.

### Test-Generation Tasks — 'Read Handler Source' Step

**Source**: `2026-07-29-18.30.md` (integration tests guessed status codes)
**Rule**: When a plan task generates integration tests, include the step: "Read the handler source and derive test expectations (status codes, response shapes, route patterns) before writing assertions." Direct DB seeding tasks must note that all handler-created relationships must be replicated.

### Contract Work — ADR Boundary Check + Contract-First CI

**Source**: `2026-07-31-12.29.md` (GraphQL schema diverged from ADR)
**Rule**: When planning GraphQL schema or API contract work, include a task to cross-check the ADR forbidden-list and the E2E boundary spec, and run contract-first tests in CI immediately after the contract is aligned — they catch missing implementation.
