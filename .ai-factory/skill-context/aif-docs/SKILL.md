# Project Rules for /aif-docs

> Auto-generated by `$aif-evolve`. Do not edit manually.
> Last updated: 2026-08-02 00:25
> Based on: 9 analyzed patches (2026-07-19-18.30.md, 2026-07-20-00.00.md, 2026-07-21-12.24.md, 2026-07-23-00.02.md, 2026-07-23-16.10.md, 2026-07-24-12.00.md, 2026-07-26-22.17.md, 2026-08-01-20.53.md, 2026-08-01-23.51.md)

## Rules

### Document Docker Compose Selection for Testing

**Source**: `2026-07-20-00.00.md` (JWT key mismatch), `2026-07-19-18.30.md` (Go version mismatch)
**Rule**: When documenting deployment or test setup, include a table mapping which `docker-compose*.yml` file to use for each scenario:

| Scenario | Compose File | Key Difference |
|----------|-------------|----------------|
| Development | `deploy/docker-compose.yml` | Uses Keycloak JWKS for auth validation |
| E2E Testing | `deploy/docker-compose.test.yml` | Uses `JWT_DEV_PUBLIC_KEY_PEM` for self-signed test tokens |
| CI Pipeline | `deploy/docker-compose.test.yml` | Same as E2E — self-signed tokens match CI test JWTs |

Document that the test compose file sets `JWT_DEV_PUBLIC_KEY_PEM` on the API Gateway, which takes highest priority in the auth resolution chain (`keyfunc.go`). The `webServer.url` in Playwright configs must use port 3000 (frontend nginx proxy), not port 8080 (API Gateway direct).

### Document .env.dev Setup

**Source**: `2026-07-23-00.02.md` (KC_HOSTNAME override in untracked .env.dev), `2026-07-21-12.24.md` (wrong compose file for E2E)
**Rule**: When documenting development environment setup, include a reference that `.env.dev.example` (or equivalent) should exist with commented-out domain-specific overrides. Document that `KC_HOSTNAME=localhost` is the default dev value and any override must resolve in the developer's environment. Include the `KEYCLOAK_JWKS_URL` endpoint URL in the documented variables. Also document that `docker compose -f deploy/docker-compose.test.yml` must be used for E2E tests (not the base compose file alone), and that JWT test tokens must be regenerated when expired.

## Generalized Principles (2026-07-23 → 2026-08-01)

### Deployment: Compose Port Strategy + Env Sync

**Source**: `2026-07-24-12.00.md` (expose vs ports drift), `2026-07-26-22.17.md` (docker compose restart)
**Rule**: When documenting deployment/ports: internal services use `expose` (bridge-network only, NOT host `ports`) per the ADR port-mapping strategy; all three `.env.*` files (dev/test/staging) must stay in sync; document that `docker compose restart` does NOT re-read environment variables — `docker compose up -d --force-recreate` is required after compose file changes.

### Dev Auth Mechanism (Mirror E2E)

**Source**: `2026-08-01-20.53.md` (dev JWT minting)
**Rule**: When documenting dev-environment auth, describe the dev overlay (`deploy/docker-compose.dev.yml`) that sets `JWT_DEV_PUBLIC_KEY_PEM` on the gateway and the host-side JWT minting step (`deploy/dev-jwt/gen-dev-jwt.js`, node + jsonwebtoken + `tests/e2e/scripts/test-jwt-key.pem`) — the dev mechanism mirrors the E2E one. Note the signing key stays on the host; only the minted token enters the container.

### Design System Docs Stay in Sync

**Source**: `2026-07-23-16.10.md` (README listed never-created components)
**Rule**: When documenting the design system, keep `design/README.md`'s component list in sync with `design/ui-kit.lib.pen` — the doc and the file must not diverge (documented components must exist in the library).

### Localization Scope

**Source**: `2026-08-01-23.51.md` (page-level localization)
**Rule**: When documenting frontend localization: UI chrome (buttons, headings, placeholders, aria-labels) is localized via `t()`; API-provided data (widget titles, activity feed, demo project names) is intentionally NOT localized client-side — document this boundary so readers don't expect translation of server content.
