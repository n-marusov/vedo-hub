# Project Rules for /aif-fix

> Auto-generated by `$aif-evolve`. Do not edit manually.
> Last updated: 2026-07-18 18:30
> Based on: 3 analyzed patches (2026-07-14-20.30.md, 2026-07-16-18.30.md, 2026-07-18-18.23.md)

## Rules

### Security Fix — Test Requirement

**Source**: `2026-07-18-18.23.md` — M2 AI Ontology Creation (X-Admin-Override, X-Ontology-Visibility bypasses)
**Rule**: When fixing a security vulnerability (auth bypass, RBAC, input validation, prompt injection), include a test that proves the bypass is closed. Add it to `tests/security/` for auth bypass tests, or the service's test suite for service-specific security tests. The test must dispatch a real HTTP request (not a mock returning `ExpectedStatus`) and assert the attack is blocked.

### LLM Prompt Injection Defense Checklist

**Source**: `2026-07-18-18.23.md` — M2 AI Ontology Creation (dead code, Unicode bypass, missing length limits)
**Rule**: When fixing LLM-related code (AI handlers, prompt builders, LLM provider calls), verify:

1. All defined sanitization/validation functions (e.g., `SanitizeUserInput`, `SanitizeLLMInput`, `ValidateLLMOutput`) are wired into response paths — not dead code
2. Unicode normalization (NFC) is applied before pattern matching; zero-width characters are stripped
3. Input length limits are enforced for all user-supplied text (prompts, feedback, class IDs)
4. All user-supplied values interpolated into prompts are sanitized before reaching the LLM
5. `http.NewRequestWithContext()` is used for all outbound LLM calls with `io.LimitReader` on response body

### Query Parameterization Rule

**Source**: `2026-07-14-20.30.md` — M1 Review (SQL injection, Cypher injection, SPARQL→Cypher injection)
**Rule**: Never string-interpolate user-controlled input into SQL, Cypher, or SPARQL queries. Always use bound parameters (`$1`, `$vedo_limit`, `neo4rs::query::param`). Reject string-built queries in code review the same way as `eval()`.

### Failure Propagation Rule

**Source**: `2026-07-14-20.30.md` — M1 Review (push_state returned Ok(()) on transport failure, unwrap_or(0) swallowed DB errors)
**Rule**: Transport errors must propagate to callers. Never return `Ok(())` after a failed network/DB call. Never use `unwrap_or(default)` to silently swallow errors — it hides data integrity bugs. Check `.error_for_status()` on HTTP responses and propagate `Err` from transport failures.

### Never Trust Client Headers for Security

**Source**: `2026-07-16-18.30.md` — M2 initial (X-Admin-Override, X-Ontology-Visibility bypasses)
**Rule**: Never trust client-supplied headers for security decisions (admin status, visibility, user ID). Always verify against server-side data (JWT claims, gRPC resolver, database lookup). Client headers may serve as optimization hints (fallback when server unavailable) but never as the sole authority.

### Cross-Ontology Scope Rule

**Source**: `2026-07-16-18.30.md` — M2 initial (set_domain/set_range MATCH without ontology_id)
**Rule**: Every Neo4j MATCH clause operating on shared entity IDs must include an `ontology_id` filter. When fixing ontology-scoped queries, verify the MATCH pattern filters by `{ontology_id: $ontology_id}` — omission causes cross-ontology data corruption.
