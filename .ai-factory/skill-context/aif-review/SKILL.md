# Project Rules for /aif-review

> Auto-generated by `$aif-evolve`. Do not edit manually.
> Last updated: 2026-08-02 00:25
> Based on: 19 analyzed patches (2026-07-14-20.30.md, 2026-07-16-18.30.md, 2026-07-17-22.00.md, 2026-07-18-18.23.md, 2026-07-20-13.15.md, 2026-07-21-12.15.md, 2026-07-21-13.00.md, 2026-07-22-00.00.md, 2026-07-22-19.00.md, 2026-07-22-20.10.md, 2026-07-22-20.30.md, 2026-07-23-00.02.md, 2026-07-23-15.00.md, 2026-07-23-21.32.md, 2026-07-25-01.26.md, 2026-07-27-23.55.md, 2026-07-31-12.29.md, 2026-08-01-23.24.md, 2026-08-01-23.51.md)

## Rules

### LLM Prompt Injection Defense Review Checklist

**Source**: `2026-07-18-18.23.md` — M2 AI Ontology Creation (all 17 critical issues)
**Rule**: When reviewing LLM-related code changes, check:

1. Are `ValidateLLMOutput`/`SanitizeLLMInput`/`SanitizeUserInput` (or equivalent) called in all response paths — not merely defined as dead code?
2. Is Unicode normalization (NFC) applied before pattern matching; are zero-width characters stripped?
3. Are `MaxTextLength`/`MaxFeedbackLength` limits enforced with proper error codes (e.g., `PAYLOAD-TOO-LARGE`)?
4. Are all user-supplied values interpolated into prompts sanitized before reaching the LLM?
5. Are nil provider guards in place before calling LLM provider methods?
6. Is the gRPC auth interceptor (`authUnaryClientInterceptor`) propagating JWT tokens for inter-service calls?
7. Are outbound HTTP calls using `http.NewRequestWithContext()` with `io.LimitReader` (10MB) on response body?

### Query + Error Handling Review Checklist

**Source**: `2026-07-14-20.30.md` — M1 Review (SQL injection, Cypher injection, SPARQL→Cypher injection, error swallowing)
**Rule**: When reviewing code that interacts with databases or external services, check:

1. Are all queries parameterized (no string interpolation of user input)?
2. Are transport errors propagated (no `unwrap_or(default)` or `Ok(())` on failure)?
3. Are client-supplied headers (`X-User-Id`, `X-Admin-Override`) verified server-side?
4. Do all Neo4j MATCH clauses on entities include `ontology_id` filter?

### Docker + Migration Review Checklist

**Source**: `2026-07-17-22.00.md` — Docker Compose build fixes
**Rule**: When reviewing Docker/compose changes, check:

1. Verify Rust workspace Dockerfiles use workspace-root build context
2. Verify Go Dockerfiles copy sibling modules for `replace` directives
3. Verify `docker compose config` passes on any compose change
When reviewing code migration between services, verify the destination compiles (`go vet`/`cargo check`).

### WCAG AA Contrast Review for CSS Changes

**Source**: `2026-07-20-13.15.md` (color contrast violation on login page)
**Rule**: When reviewing CSS/frontend changes that introduce or modify color values, verify that color tokens on dark backgrounds meet WCAG AA contrast thresholds (4.5:1 for normal text, 3:1 for large text). Pay special attention to design token pairs such as `--muted-foreground` on `--card` (`#101010`) and `--background` (`#0a0a0a`). Flag any color combinations with contrast below 4.5:1 as WARN level in the review report.

### Frontend Auth Flow Review

**Source**: `2026-07-21-12.15.md` (JWT not synced to localStorage), `2026-07-23-00.02.md` (Keycloak OIDC claims mismatch)
**Rule**: When reviewing frontend auth changes: (a) for every `localStorage.getItem("vedo-jwt-token")` call, trace whether a corresponding `setItem` exists in the login/refresh flow; (b) verify JWT parsing handles standard OIDC claim names (`sub`, `realm_access.roles`) alongside custom mappers.

### Test Accuracy Review

**Source**: `2026-07-21-13.00.md` (test expectations using inferred text), `2026-07-22-00.00.md` (fill() after navigation without wait)
**Rule**: When reviewing test changes: (a) test expectations must match actual component template text — verify by reading the template, not inferring; (b) dialog form tests must satisfy ALL validation checks (required field assertions) before asserting submit behavior; (c) flag any `fill()` call after navigation that lacks a preceding `waitFor({ state: 'visible' })` or equivalent page-object method that performs the wait.

### Makefile & Build Variable Consistency

**Source**: `2026-07-22-19.00.md` (Makefile ENV default), `2026-07-22-20.10.md` (docker.mk undefined variables)
**Rule**: When reviewing Makefile or Docker build changes: (a) flag any `$(VAR)` reference that lacks a `?=` default when `--warn-undefined-variables` is enabled; (b) verify `--build-arg NAME=$(NAME)` matches the Dockerfile `ARG NAME` exactly — mismatched args silently pass empty values.

### Playwright Locator Quality & Side Effects

**Source**: `2026-07-22-20.30.md` (race condition in getErrorMessage), `2026-07-22-00.00.md` (POM tab navigation gap)
**Rule**: When reviewing Playwright page objects: (a) flag any READ method that has side effects (clicks, form fills); (b) flag any locator that could match UI elements in multiple dialog/visibility states without a parent scope like `getByRole('dialog')`; (c) prefer role-based locators (`getByRole('button', { name: /pattern/i })`) over CSS classes for tab switches.

### Vue Reactive State Shadow Declaration

**Source**: `2026-07-23-15.00.md` (duplicate const counters declarations in MetricsPage.vue)
**Rule**: When reviewing Vue `<script setup>` refactoring, check for duplicate `const` declarations with the same name in the same block. Search for the variable name across the entire `<script setup>` section — the SFC compiler catches this at build time but dev mode may mask it.

## Generalized Principles (2026-07-23 → 2026-08-01)

### Auth Contract Review — Method-Aware BFLA + IDP Claim Normalization

**Source**: `2026-07-23-21.32.md` (GET /groups required Owner), `2026-07-25-01.26.md` (Keycloak lowercase roles)
**Rule**: When reviewing auth/BFLA gates that classify paths as admin-only, flag any classification that ignores the HTTP method — GET endpoints on management paths must not require Owner unless the data itself is admin-only; fine-grained checks belong to the auth-service. Verify role lookups normalize the IDP's actual claim format (Keycloak realm roles are lowercase) and that tests use the IDP's real format, not hardcoded PascalCase.

### Contract Drift Review Across Protocol Layers

**Source**: `2026-07-31-12.29.md` (GraphQL schema/proto/gateway drift)
**Rule**: When reviewing GraphQL/API changes, verify proto/spec enum additions are reflected in ALL layers (domain, REST gateway validation, GraphQL, docs), and that new resolvers respect the ADR forbidden-list and the E2E schema-boundary spec. Flag stale docs (`graphql-schema.md`) that omit contract features.

### TypeScript Config Review

**Source**: `2026-07-27-23.55.md` (tsconfig exclude broke alias resolution)
**Rule**: When reviewing TypeScript changes that add test files, flag any `tsconfig.json` `exclude` containing `__tests__` — it breaks `@/` alias resolution and hides type errors in test files. "Cannot find module" for existing files is a config issue, not a missing file.

### i18n Hardcoded Strings in Shared Components

**Source**: `2026-08-01-23.24.md` (English shell), `2026-08-01-23.51.md` (raw key rendering)
**Rule**: When reviewing frontend changes, flag literal English strings in `<template>` of shell/global components — all UI text must route through `t()`. Verify referenced keys exist in BOTH `ru.json` and `en.json` (missing keys render as the raw key string) and that the two locale files keep key parity.
