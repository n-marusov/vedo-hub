# P0 Requirements Triage: Orphan Analysis

> Generated by $aif-test-quality
> Date: 2026-07-23
> Scope: 171 orphan P0 REQs in traceability.ttl without `vdo:validates` links

## Summary

| Category | Count | Action |
|----------|-------|--------|
| 🟢 Already tested — needs TTL link | ~25 | Quick win — add `vdo:validates` |
| 🟡 Testable — needs new tests | ~80 | Requires implementation |
| 🔴 Non-testable (constraint/process/policy) | ~66 | No test needed — mark as non-testable |

**Recommendation:** Start with 🟢 quick wins (add TTL links), then triage 🟡 by impact.

---

## 🟢 Quick Wins — Tests Exist, Just Need TTL Links

These REQs have correlating test files that already test the described behavior.
Add `vdo:validates` triples to `traceability.ttl`.

### Document Extraction (REQ-FUN.API.doc-extract-*, excel-*)

The `document-extractor` service has 10 test files covering parsers (csv, docx, json, pdf, text, xlsx, xml), API routes, and LLM client. These REQs describe the same functionality:

| REQ | Test file | TTL action |
|-----|-----------|------------|
| `REQ-FUN.API.doc-extract-sequence-schema` | `src/services/document-extractor/sequence/validator_test.py` | Add link |
| `REQ-FUN.API.doc-extract-supported-formats` | `src/services/document-extractor/parsers/*_test.py` (7 parsers) | Add link |
| `REQ-FUN.API.excel-duplicate-handling` | `src/services/document-extractor/parsers/xlsx_parser_test.py` | Add link |
| `REQ-FUN.API.excel-error-threshold` | `src/services/document-extractor/parsers/xlsx_parser_test.py` | Add link |
| `REQ-FUN.API.excel-import-block` | `src/services/document-extractor/parsers/xlsx_parser_test.py` | Add link |
| `REQ-FUN.API.excel-supported-formats` | `src/services/document-extractor/parsers/xlsx_parser_test.py` | Add link |
| `REQ-FUN.API.excel-validation` | `src/services/document-extractor/parsers/xlsx_parser_test.py` | Add link |
| `REQ-NFR.API.doc-extract-performance` | `src/services/document-extractor/parsers/*_test.py` | Add link |

### Ontology OWL/Syntax (REQ-FUN.API.owl-*)

The `ontology-service` has 9 integration test files covering class CRUD, property CRUD, validation, graph queries. These REQs map to existing tests:

| REQ | Test file | TTL action |
|-----|-----------|------------|
| `REQ-FUN.API.owl-syntax-validity` | `src/services/ontology-service/tests/validation_integration_test.rs` | Add link |
| `REQ-FUN.API.owl-unique-iri` | `src/services/ontology-service/tests/class_integration_test.rs` | Add link |
| `REQ-FUN.API.owl-reference-integrity` | `src/services/ontology-service/tests/property_integration_test.rs` | Add link |
| `REQ-FUN.API.no-cyclic-hierarchy` | `src/services/ontology-service/tests/class_integration_test.rs` | Add link |
| `REQ-FUN.API.write-idempotency` | `src/services/api-gateway/rest_entity_crud_integration_test.go` | Add link |
| `REQ-FUN.API.rest-versioning` | `src/services/api-gateway/rest_entity_crud_integration_test.go` | Add link |
| `REQ-FUN.API.graphql-deprecation-policy` | `tests/e2e/playwright/tests/api/graphql/schema-boundary.spec.ts` | Add link |
| `REQ-FUN.API.unified-root-endpoint` | `src/services/api-gateway/main_test.go` | Add link |
| `REQ-FUN.API.properties-accuracy` | `src/services/ontology-service/tests/property_integration_test.rs` | Add link |

### Versioning (REQ-FUN.DATA.*) — Some Already Tested

The `versioning-service` has 8 integration tests. These REQs describe versioning/DATA behavior:

| REQ | Test file | TTL action |
|-----|-----------|------------|
| `REQ-FUN.DATA.query-export-content` | `src/services/api-gateway/query_integration_test.go` | Add link |
| `REQ-FUN.DATA.query-export-format` | `src/services/api-gateway/query_integration_test.go` | Add link |
| `REQ-FUN.DATA.query-export-validation` | `src/services/api-gateway/query_integration_test.go` | Add link |

---

## 🟡 Testable — Needs New Tests

These describe testable behavior but currently have no test coverage:

| Area | Count | Examples | Test approach |
|------|-------|----------|---------------|
| REQ-FUN.API (remaining) | 8 | `suggestion-*`, `gradual-rollback`, `on-premise-lts`, `iterative-refinement-context`, `validation-import`, `validation-pr`, `protocol-stack`, `swagger-ui-dev-only` | Add unit/integration tests |
| REQ-NFR.SECURITY | 17 | `audit-*`, `emergency-policy-disable`, `llm-*`, `prompt-*`, `privileged-access-control`, `parser-query-fuzz-gates`, `suggestion-privacy` | Security test files in `tests/security/` |
| REQ-NFR.API | 10 | `backward-compatibility`, `circuit-breaker`, `generation-latency`, `suggestion-latency`, `validation-latency`, `sparql-compatibility`, `excel-max-size` | Add API benchmark/latency tests |
| REQ-FUN.INTEGRATION | 8 | `collaboration`, `ticket-management`, `audit-log-content`, `billing-*`, `credit-top-up`, `provider-billing-integration` | Integration test suites |
| REQ-USR.UI | 24 | `graph-navigation`, `import-export`, `confidence-indicator`, `sparql-gui-search`, `doc-extract-upload`, `customization`, `nx-*` | Frontend specs |
| REQ-NFR.UI | 3 | `accessibility`, `error-feedback`, `data-loss-prevention` | Frontend + a11y specs |
| REQ-NFR.PERF | 1 | `performance` | Performance benchmarks |
| REQ-FUN.DATA (remaining) | 5 | `gdpr-export-mandate`, `migration-*`, `storage-stack` | Integration tests |
| REQ-FUN.OPS | 1 | `credit-zero-block` | API integration test |
| REQ-USR.API | 1 | `deprecation-notification` | API test |
| REQ-NFR.OPS (partially) | 3 | `llm-agent-observability`, `observability-stack`, `metrics` | Can verify prometheus endpoints |
| REQ-FUN.INFRA.vedo-cli-specification | 1 | CLI specification | CLI tests exist in `src/cli/` |

Maximum priority impact: **NFR-SECURITY** (already planned for M3) and **FUN-API** (core functional behavior without tests).

---

## 🔴 Non-Testable — Constraint / Process / Policy

These describe constraints, process requirements, or business policies that cannot be verified through automated tests:

| Area | Count | Reason |
|------|-------|--------|
| REQ-CON.* | 15 | Deployment constraints, stack decisions, architecture constraints (e.g., `REQ-CON.INFRA.deployment-model`, `REQ-CON.STACK.backend-service-stack`) |
| REQ-FUN.PROCESS.* | 10 | CI/CD procedures, deployment checklist, escalation matrix, incident response, linting config |
| REQ-NFR.PROCESS.* | 3 | Chaos testing, rollout safety gates (process documents, not code) |
| REQ-NFR.DATA.* (majority) | 12 | Backup/retention policies, decommission policies, rollback RPO |
| REQ-NFR.INFRA.* | 7 | Availability SLO, client environment, control-plane isolation, router resilience, runbook |
| REQ-NFR.OPS.* (majority) | 4 | Log retention, provider alerting, provider health checks, critical alerts |
| REQ-NFR.SUP.* | 1 | Support SLA (contractual) |
| REQ-BIZ.SUP.* | 3 | Business billing, credit reporting, modular billing |
| REQ-NFR.INTEGRATION.* | 2 | Failover internal-private, max-downtime (infrastructure) |
| REQ-CROSS.* | 1 | Sequences constraint |
| REQ-FUN.DOC.* | 1 | LLM privacy policy |
| REQ-USR.DOC.* | 1 | User guide navigation |
| REQ-FUN.INFRA.* (except vedo-cli) | 3 | Deployment integrity, provider-priority-config, runbook procedures |

These should be annotated as `vdo:nfr` with `vdo:testStrategy "documented"` (or similar marker) to distinguish from unaddressed testable P0s.

---

## Priority Action Plan

### Phase 1 (Next sprint) — Add TTL links for 🟢 quick wins
Add `vdo:validates` for ~25 REQs that already have test coverage but lack TTL links.
Re-compute RCS → should increase from ~0.8 to ~4.0+.

### Phase 2 (This milestone) — Security test coverage (NFR-SECURITY)
17 orphan P0 security REQs. Most are already planned for M3 security gates
(`tests/security/authorization/`). Link tests as they're implemented.

### Phase 3 (Next milestone) — API functional gaps (FUN-API)
8 remaining FUN-API orphan P0s that have no test coverage:
- `suggestion-confidence-threshold`, `suggestion-recalculation` → AI-orchestration service
- `iterative-refinement-context`, `gradual-rollback` → API Gateway
- `validation-import`, `validation-pr` → Ontology service
- `protocol-stack` → Integration test
- `on-premise-lts` → Not testable per-se (LTS policy)

### Phase 4 (Backlog) — USR-UI specs
24 orphan P0 UI REQs. The frontend has 28 specs covering `gui-implementation`,
but specific UI behaviors (graph-navigation, import-export, confidence-indicator)
need dedicated spec files.

---

## Machine-Readable Summary

```
<!-- rcs-triage: testable-existing=25 | testable-needed=80 | non-testable=66 -->
<!-- priority-p0-orphans: 171 total / 66 non-testable / 80 need-tests / 25 need-links -->
```
