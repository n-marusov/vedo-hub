# Evolution: 2026-07-14 10:45

## Intelligence Summary
- Patches analyzed: 2
- Recurring patterns: build ≠ type check (appears in both patches: 01.10 with Rust/Go, 10.31 with Vue TS6133)
- Tech stack: Vue 3 + TypeScript, Go, Rust — each has its own type/lint gate that build doesn't cover

## Improvements Applied

### aif-implement → skill-context
- Build ≠ Type Check — drives cargo check / go vet / vue-tsc --noEmit as verification gates
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Template Rewrite Audit — audit imports and bindings after Vue template rewrites
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Destructure Only What You Use — avoid unused destructured vars in composables
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Omit Unused v-for Index — avoid unused index bindings in Vue v-for
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- .env.example Sync — keep env docs in sync with code
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- GraphQL End-to-End — build schema + resolver + frontend query in one change
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`
- Gin Router Trie Safety — never register static + wildcard at same trie level
  **File:** `.ai-factory/skill-context/aif-implement/SKILL.md`

## Patterns Identified
- **build ≠ type check**: 2 occurrences — one in Rust/Go (01.10), one in Vue/TS (10.31). Same root pattern: developer assumes successful build means correct code. Different manifestations: dead_code warnings in Rust, unused imports in Vue, env vars in Go.
